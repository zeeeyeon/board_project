server:
  port: 8080

spring:
  application:
    name: board_project

  data:
    mongodb:
      uri: mongodb://localhost:27017/board

    redis:
      host: localhost
      port: 6379

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: always
